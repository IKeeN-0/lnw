syntax = "proto3";

package todo;

service TodoService{
    rpc AddTodo (TodoList) returns (TodoList){}
    rpc ReadTodo (Empty) returns (TodoList) {}
}

message Empty{}

message TodoList{
    string name = 1;
    string do = 2;
}